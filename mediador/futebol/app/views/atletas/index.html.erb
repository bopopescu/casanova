<h1>Listing atletas</h1>

<table>
  <tr>
    <th>Apelido</th>
    <th>Nome</th>
    <th>Nascimento</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @atletas.each do |atleta| %>
  <tr>
    <td><%= atleta.apelido %></td>
    <td><%= atleta.nome %></td>
    <td><%= atleta.nascimento %></td>
    <td><%= link_to 'Show', atleta %></td>
    <td><%= link_to 'Edit', edit_atleta_path(atleta) %></td>
    <td><%= link_to 'Destroy', atleta, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Atleta', new_atleta_path %>
